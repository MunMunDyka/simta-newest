/**
 * ===========================================
 * Services Index
 * ===========================================
 * Export all service modules
 */

'use strict';

const whatsappService = require('./whatsappService');

module.exports = {
    whatsappService
};
