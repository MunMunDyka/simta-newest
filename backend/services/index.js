/**
 * ===========================================
 * Services Index
 * ===========================================
 * Export all service modules
 */

'use strict';

const emailService = require('./emailService');

module.exports = {
    emailService
};
